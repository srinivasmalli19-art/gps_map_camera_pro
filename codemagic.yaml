workflows:

  # ── Android Release APK ─────────────────────────────────────────────────────
  android-release:
    name: Android Release APK
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      flutter: stable
      java: 17
    scripts:
      - name: Get Flutter packages
        script: flutter pub get

      - name: Build release APK
        script: flutter build apk --release

    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk

    publishing:
      email:
        recipients:
          - srinivasmalli19@gmail.com
        notify:
          success: true
          failure: true

  # ── Android Debug APK ───────────────────────────────────────────────────────
  android-debug:
    name: Android Debug APK
    max_build_duration: 45
    instance_type: mac_mini_m2
    environment:
      flutter: stable
      java: 17
    scripts:
      - name: Get Flutter packages
        script: flutter pub get

      - name: Build debug APK
        script: flutter build apk --debug

    artifacts:
      - build/app/outputs/flutter-apk/app-debug.apk

    publishing:
      email:
        recipients:
          - srinivasmalli19@gmail.com
        notify:
          success: true
          failure: true

  # ── iOS Release IPA ─────────────────────────────────────────────────────────
  # Requires Apple Developer account + signing certificates in Codemagic.
  # Steps to enable:
  #   1. Add your Apple Developer account in Codemagic Team > Integrations
  #   2. Upload Distribution certificate + Provisioning profile in Code Signing
  #   3. Uncomment this entire workflow block
  #
  # ios-release:
  #   name: iOS Release IPA
  #   max_build_duration: 120
  #   instance_type: mac_mini_m2
  #   environment:
  #     flutter: stable
  #     java: 17
  #     xcode: latest
  #     cocoapods: default
  #     ios_signing:
  #       distribution_type: app_store
  #       bundle_identifier: com.srinivasmalli.gpsmapscamerapro
  #   scripts:
  #     - name: Get Flutter packages
  #       script: flutter pub get
  #     - name: Install CocoaPods
  #       script: cd ios && pod install
  #     - name: Build IPA
  #       script: flutter build ipa --release --export-options-plist=/Users/builder/export_options.plist
  #   artifacts:
  #     - build/ios/ipa/*.ipa
  #   publishing:
  #     email:
  #       recipients:
  #         - srinivasmalli19@gmail.com
  #       notify:
  #         success: true
  #         failure: true
